<h1>Guide to making your own expense tracker</h1>

1. Create empty spreadsheet
1. Click "Google Apps Script", paste in `Code.js`, run as Web API, copy API key
1. Fork the repository
1. Change API key in `index.html` to copied API key
1. Enable Github Pages
